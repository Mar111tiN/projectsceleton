
(
    pl <- sarah_plot(stat.file, q.file, box.width = 0.65, jitter=0.1, text.size=22, point.size=3, pop.colors=c(
        "darkgray",
        "#C00000",
        "darkgray",
        "darkgray"
    ))
)
